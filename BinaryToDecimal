int Binarytodecimal(int bin){
    int ans = 0, pow = 1;
    
    while(bin > 0){
        int rem = bin % 10;
        bin /= 10;
        ans += rem * pow;
        pow *= 2;
        
    }
    
    return ans;
}
int main() {
    int a = Binarytodecimal(1000);
    cout << a << endl;

    return 0;
}
